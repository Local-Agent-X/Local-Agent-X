{
  "description": "Files and directories the agent can NEVER modify via the edit/write/delete_file tools. These are the engine core — if they break, the agent can't run to fix itself. A trailing slash means the whole directory tree is protected. (self_edit is a separate, gated path and may touch these.)",
  "protected": [
    "src/index.ts",
    "src/server/",
    "src/chat-ws/",
    "src/canonical-loop/",
    "src/agent-request/",
    "src/tool-execution/",
    "src/tool-executor.ts",
    "src/anthropic-client/",
    "src/anthropic-models.ts",
    "src/codex-client/",
    "src/self-edit-sandbox.ts",
    "src/self-edit-sandbox-gates.ts",
    "src/auth/",
    "src/config.ts",
    "src/config-loader.ts",
    "src/context/",
    "src/context-manager/",
    "src/types.ts",
    "src/security/",
    "src/sanitize.ts",
    "src/sandbox/",
    "src/secrets.ts",
    "src/db-migrations.ts",
    "src/ari-kernel/",
    "config/protected-files.json",
    "config/system-prompt.md"
  ],
  "reason": {
    "src/index.ts": "Server bootstrap — breaks = nothing starts",
    "src/server/": "HTTP/route wiring — breaks = no endpoints",
    "src/chat-ws/": "WebSocket layer — breaks = can't talk to agent",
    "src/canonical-loop/": "Consolidated agent execution loop — breaks = can't think (replaced the old agent-providers/agent-request monoliths)",
    "src/agent-request/": "Request pipeline — breaks = can't process messages",
    "src/tool-execution/": "Tool dispatch pipeline — breaks = can't use tools",
    "src/tool-executor.ts": "Tool runner entrypoint — breaks = can't use tools",
    "src/anthropic-client/": "Claude API client — breaks = can't reach LLM",
    "src/anthropic-models.ts": "Model registry — breaks = can't pick a model",
    "src/codex-client/": "Codex/OpenAI-compat client — breaks = can't reach that provider",
    "src/self-edit-sandbox.ts": "Self-edit safety gate — agent breaking this would brick the safety net",
    "src/self-edit-sandbox-gates.ts": "Self-edit safety gate (build/bind/smoke implementations) — same",
    "src/auth/": "Auth + OAuth — breaks = locked out / can't authenticate",
    "src/config.ts": "Config loader — breaks = can't start",
    "src/config-loader.ts": "Config file reader — breaks = can't load config",
    "src/context/": "Prompt assembly — breaks = empty/garbage prompts",
    "src/context-manager/": "Context window management — breaks = broken long conversations",
    "src/types.ts": "Type definitions — breaks = compile errors everywhere",
    "src/security/": "Security layer — breaks = no guardrails",
    "src/sanitize.ts": "Input sanitization — breaks = injection risk",
    "src/sandbox/": "Sandbox enforcement — breaks = no isolation",
    "src/secrets.ts": "Secrets manager — breaks = exposed credentials",
    "src/db-migrations.ts": "Database schema — breaks = data corruption",
    "src/ari-kernel/": "ARI kernel — breaks = no policy enforcement",
    "config/protected-files.json": "This file — agent can't remove its own guardrails",
    "config/system-prompt.md": "Global agent prompt. Per-user preferences and facts go to MEMORY, not here — use `remember`, `memory_set_user_field`, or `memory_update_profile` instead. Editing the system prompt for user-specific content pollutes every future conversation with every user."
  }
}
