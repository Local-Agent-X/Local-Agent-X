{
  "description": "Files the agent can NEVER modify. These are the engine core — if they break, the agent can't run to fix itself.",
  "protected": [
    "src/index.ts",
    "src/server.ts",
    "src/chat-ws.ts",
    "src/agent-providers.ts",
    "src/agent-request.ts",
    "src/agent-codex.ts",
    "src/anthropic-client.ts",
    "src/anthropic-models.ts",
    "src/codex-client.ts",
    "src/tool-executor.ts",
    "src/self-edit-sandbox.ts",
    "src/self-edit-sandbox-gates.ts",
    "src/auth.ts",
    "src/auth-anthropic.ts",
    "src/config.ts",
    "src/config-loader.ts",
    "src/context-builder.ts",
    "src/context-manager.ts",
    "src/types.ts",
    "src/security.ts",
    "src/sanitize.ts",
    "src/sandbox.ts",
    "src/secrets.ts",
    "src/db-migrations.ts",
    "src/ari-kernel.ts",
    "config/protected-files.json",
    "config/system-prompt.md"
  ],
  "reason": {
    "src/index.ts": "Server bootstrap — breaks = nothing starts",
    "src/server.ts": "HTTP/route wiring — breaks = no endpoints",
    "src/chat-ws.ts": "WebSocket — breaks = can't talk to agent",
    "src/agent-providers.ts": "Agent execution loop — breaks = can't think",
    "src/agent-request.ts": "Request pipeline — breaks = can't process messages",
    "src/anthropic-client.ts": "Claude API client — breaks = can't reach LLM",
    "src/tool-executor.ts": "Tool runner — breaks = can't use tools",
    "src/self-edit-sandbox.ts": "Self-edit safety gate — agent breaking this would brick the safety net",
    "src/self-edit-sandbox-gates.ts": "Self-edit safety gate (build/bind/smoke implementations) — same",
    "src/auth.ts": "Auth — breaks = locked out",
    "src/auth-anthropic.ts": "OAuth — breaks = can't authenticate",
    "src/config.ts": "Config loader — breaks = can't start",
    "src/config-loader.ts": "Config file reader — breaks = can't load config",
    "src/context-builder.ts": "Prompt assembly — breaks = empty prompts",
    "src/types.ts": "Type definitions — breaks = compile errors everywhere",
    "src/security.ts": "Security layer — breaks = no guardrails",
    "src/sanitize.ts": "Input sanitization — breaks = injection risk",
    "src/sandbox.ts": "Sandbox enforcement — breaks = no isolation",
    "src/secrets.ts": "Secrets manager — breaks = exposed credentials",
    "src/db-migrations.ts": "Database schema — breaks = data corruption",
    "src/ari-kernel.ts": "ARI kernel — breaks = no policy enforcement",
    "config/protected-files.json": "This file — agent can't remove its own guardrails",
    "config/system-prompt.md": "Global agent prompt. Per-user preferences and facts go to MEMORY, not here — use `remember`, `memory_set_user_field`, or `memory_update_profile` instead. Editing the system prompt for user-specific content pollutes every future conversation with every user."
  }
}
