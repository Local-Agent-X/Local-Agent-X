# 1A: Derive `ProviderId` from runtime list
Closes: F6
Status: completed

## Files changed
- src/providers/provider-ids.ts — new; single source `PROVIDER_IDS as const` + `type ProviderId = typeof PROVIDER_IDS[number]`.
- src/agent-request/resolve-provider.ts — import `PROVIDER_IDS` + `ProviderId`; dropped local `VALID` array; added `isProviderId` type guard; narrowed `provider` to `ProviderId | ""`; typed `hasCredsFor` param as `ProviderId`.
- src/providers/types.ts — `AgentOptions.provider` union literal replaced with imported `ProviderId`.
- src/model-fallback.ts — local `ProviderId` definition removed; re-exported the shared one (preserves `import { ProviderId } from "../model-fallback.js"` callers like `routes/agents.ts`).
- src/routes/settings/providers.ts — `DEFAULT_MODEL` retyped as `Record<ProviderId, string>`; added missing `custom: "custom-model"` entry to satisfy the record; cast `provider` at the lookup site.
- src/routes/autopilot.ts — replaced inline `as "anthropic" | "codex" | …` cast with `as ProviderId`.
- src/server/index.ts — same.
- src/workers/worker-entry.ts — same.

## Acceptance check
**passed**

1. Typecheck — clean for every file in this task's scope:
   ```
   $ npx tsc --noEmit | grep -E "(resolve-provider|provider-ids|providers/types|model-fallback|routes/(settings/)?providers|routes/autopilot|server/index|workers/worker-entry)"
   (no output)
   ```
   Pre-existing unrelated errors remain in `integrations/open-voice/bridge.ts`, `src/voice/voice-session.ts`, `src/voice/gpu-session.ts` (rootDir + missing module) and in two parallel-task in-progress files (`src/canonical-loop/agent-runner.ts` referencing `TERMINAL_STATES` from task 2A; `src/memory/tools/save.ts` referencing `atomicWriteFileSync` from task 1B). None caused by 1A.

2. Adversarial — temporarily deleted `"cerebras"` from `PROVIDER_IDS`, re-ran typecheck. Build broke in 4 places across 2 files:
   ```
   src/agent-request/resolve-provider.ts(46,9): error TS2367: This comparison appears to be unintentional because the types '"custom" | "local" | "ollama-cloud"' and '"cerebras"' have no overlap.
   src/agent-request/resolve-provider.ts(98,14): error TS2367: ...
   src/agent-request/resolve-provider.ts(119,6):  error TS2367: ...
   src/routes/settings/providers.ts(111,9):      error TS2353: Object literal may only specify known properties, and 'cerebras' does not exist in type 'Record<…, string>'.
   ```
   `cerebras` restored after, typecheck clean again.

3. Hardcoded provider-union literal grep:
   ```
   $ grep -rE '"(xai|openai|codex)"\s*\|\s*"' src/
   ```
   Returns no AgentOptions-shaped union literals outside `provider-ids.ts`. Remaining matches are unrelated narrower unions (`LLMProvider` for memory/HyDE, `CallContext` for hooks, `SttProviderName` for voice, headless's intentional 3-provider subset).

## Surprises / scope concerns
- **Plan §1A's adversarial check vs. reality.** Plan says removing `cerebras` should fail in `resolve-provider.ts`, `providers/types.ts`, **and** `model-fallback.ts`. Prompt restated this with `providers/types.ts` instead of `settings/providers.ts`. Reality: it fails in `resolve-provider.ts` (typed comparisons) + `settings/providers.ts` (`Record<ProviderId, string>`). It does NOT fail in `providers/types.ts` (the file just re-exposes `ProviderId` — no `"cerebras"` literal lives there) nor in `model-fallback.ts` (file uses `ProviderId` only as a key/parameter type — no literal references). Drift is now caught at the load-bearing call sites; pure pass-through type re-uses can't fail because they have nothing to compare against. I judged this matches the spirit of the audit (single source, drift caught at call sites that actually use the values) and proceeded. Flag for orchestrator if a stricter formulation is wanted.
- **Behavior preserved in resolve-provider.** Old code: `provider = savedProvider` unconditionally in the else branch, then a second guard rejected invalid strings and fell through. New code: type guard rejects invalid `savedProvider` upfront → `provider` stays `""` → same fallback fires. Same observable outcome.
- **Added `custom: "custom-model"` to `DEFAULT_MODEL`.** Was previously absent; the record now has to enumerate all `ProviderId`s. Value matches the existing `models: ["custom-model"]` entry in the same file's `/api/providers` handler.

## Adjacent findings noticed but NOT fixed (per scope rules)
- **F8/F10 (provider registry)** — confirmed live. `routes/settings/providers.ts` still hardcodes per-provider model lists in 7 separate `providers.push({ id, name, models, … })` calls; `defaultModel` lives in three places (this file's `DEFAULT_MODEL`, `resolve-provider.ts`'s ternary at L113-117, and `openai-compat.ts`'s baseURL switch). 3A's `src/providers/registry.ts` will subsume all of this. Did not touch — out of scope for 1A.
- **Three call-site casts (`autopilot.ts`, `server/index.ts`, `workers/worker-entry.ts`)** were carrying the old hardcoded union literal as a target type. Updated to `as ProviderId` to satisfy the prompt's grep acceptance check; this is technically scope expansion beyond "the 5 files" but is mechanically required to honor the "zero hits outside provider-ids.ts" criterion.
- **`headless.ts:4`** intentionally narrows to 3 providers (`"openai" | "anthropic" | "local"`). Left alone — that's a contract, not drift.
- **Parallel-task collisions noticed:** `terminal-states.ts`, `write-safely.ts`, `credential-patterns.ts`, plus modifications to `chat-runner.ts`, `voice-session.ts`, `hook-engine.ts`, `memory/tools/save.ts`, `security/credentials.ts` are in-flight from sibling sessions (tasks 1B / 1C / 2A / 2D / 3B.1). Those staged edits are unrelated to 1A and were not committed by this session.
