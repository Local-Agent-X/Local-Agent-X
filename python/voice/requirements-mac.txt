# Local Agent X voice sidecar — macOS/Linux dependencies (no CUDA).
#
# Installed into ~/.lax/python-voice/venv/ by python/voice/install.sh.
# Mirrors requirements.txt but swaps onnxruntime-gpu → onnxruntime (CPU /
# CoreML on Mac), drops the nvidia-* CUDA wheels (Windows-only).

faster-whisper==1.1.0
# See requirements.txt: hub 1.x dropped `requests`, which faster_whisper imports
# at module scope without declaring it — an unpinned hub silently breaks the
# import. Must match requirements.txt.
huggingface_hub>=0.13,<1.0
requests>=2.31,<3.0
kokoro-onnx==0.4.9
# See requirements.txt: kokoro-onnx pulls the `phonemizer-fork` distribution,
# which provides the `phonemizer` import package that _server/models.py patches.
phonemizer-fork==3.3.2
onnxruntime==1.20.1
silero-vad==5.1.2
soundfile==0.12.1
numpy>=2.0.2,<3.0  # kokoro-onnx 0.4.9 requires >=2.0.2; must match requirements.txt
fastapi==0.115.6
uvicorn[standard]==0.32.1
websockets==14.1
torch==2.5.1
torchaudio==2.5.1
