{
  "_comment": [
    "Known plug-and-play violations, recorded 2026-07-25 when check-integration-conformance",
    "landed. This is the FIX BACKLOG, not a mute list: every entry is printed on every build,",
    "and an entry that no longer reproduces fails the build so the list can only shrink.",
    "Do NOT add entries to silence a new violation — fix the declaration or the runtime path.",
    "",
    "2026-07-25: the five steer:* entries were removed when the model-locked routing clauses",
    "were deleted from the email and calendar tool descriptions. test/tool-description-portability.test.ts",
    "is now the runtime mirror of that check.",
    "",
    "2026-07-25: authtype:google removed — google was narrowed to the endpoints an api_key can",
    "actually satisfy, so the finding no longer reproduces. The hardcoded YouTube probe that was",
    "the tell is gone too, replaced by declaration-driven probe headers."
  ],
  "known": [
    {
      "id": "transport:email",
      "note": "Empty baseUrl with smtp/imap pseudo-paths. The email_* tools are the real runtime path; the endpoints are config-UI labels. Fix: declare the transport explicitly instead of leaving it shaped like a broken HTTP integration."
    },
    {
      "id": "secret:email",
      "note": "authInstructions ask for 9 values but the integration declares only SMTP_PASS, so IMAP_PASS is resolved at runtime and can never be stored through the install path. As of C5 the modal renders one field PER DECLARED CREDENTIAL and the route persists all of them, so the install path is no longer the constraint — the declaration is. email_setup remains the only working config path. Fix (C7): declare email's real credential set and make Settings drive email_setup."
    }
  ]
}
